**/dist/**
adapters/*/package/**
**/coverage/**
node_modules/**
package-lock.json
site/**
.old/**

# JVM build outputs (Maven target/, Gradle build/) — generated, never formatted.
**/target/**
fixtures/conformance/*/build/**

# Fixture data is author-controlled and sealed by the approval hash; Prettier
# must not reformat it (see fixtures/toy-repo/README.md).
fixtures/toy-repo/**

# Prose docs are hand-authored project law (charter/design/tasks) and are
# formatted by their authors, not Prettier. Keep them out of the code formatter.
**/*.md
docs/**
.claude/**
