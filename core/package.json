{
  "name": "@crucible/core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Crucible framework core: artifacts, hashing, lint, tier, verify, substrate, adapters client.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "crucible": "dist/cli/bin.js"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.test.json",
    "build": "tsc"
  },
  "dependencies": {
    "commander": "^14.0.3",
    "yaml": "^2.9.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@crucible/fixtures": "*"
  }
}
