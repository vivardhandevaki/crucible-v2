{
  "name": "@crucible/schemas",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The shipped Crucible OpenSpec change-type bundles (crucible / crucible-bugfix / crucible-refactor). Plain files installed by copying; `crucible init` copies them into a target repo's openspec/schemas/.",
  "//": "The built core CLI loads this package under plain Node in consumer CI, so its runtime export is dist/index.js. TypeScript checks run before build, so declarations remain source-visible. The bundle DATA lives under crucible*/; src/index.ts only computes paths — it never parses or mutates the bundles (that is core's changetype/ TCB).",
  "main": "dist/index.js",
  "types": "src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc"
  },
  "devDependencies": {
    "@types/node": "^22.20.1"
  }
}
