# Dependencies
node_modules/

# Build output
dist/
*.tsbuildinfo

# JVM conformance fixtures + java-junit helper build outputs (Phase 3).
# Fixture *sources* (pom.xml, build.gradle, src/**) are committed; their
# compiled/test outputs are not.
fixtures/**/target/
fixtures/conformance/*/build/
fixtures/conformance/*/.gradle/
adapters/java-junit/**/target/

# Test / coverage
coverage/

# Editor / OS
.DS_Store
*.log

# Local, non-enforcement convenience config (charter: convenience never enforces)
local.yaml
.crucible/local.yaml
