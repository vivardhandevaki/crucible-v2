# Tasks — add-greeting

Generated on the implement side of the gate (charter: tasks live post-approval).
Present here so the fixture carries a complete, archivable change.

- [ ] Implement `greet(name)` in `src/greeting.ts`.
- [ ] Make `ORC-greeting-001` pass (`greeting::returns_hello_for_a_name`).
- [ ] Make `ORC-greeting-002` pass (`greeting::defaults_to_world_when_empty`).
