# add-greeting

OpenSpec-owned change metadata. This file and `.openspec.yaml` are written by
`openspec new change` and are **outside the Crucible approval-hash scope** (they
are not review material and OpenSpec may rewrite them — see spike-notes D1).

See `proposal.md` for intent; `oracles.md` for the judges.
