// Fixtures workspace — placeholder surface for the P0-02 scaffold.
//
// The toy repo (tests.json, a valid change bundle, and one deliberately-invalid
// bundle) lands in P0-03 under fixtures/toy-repo/. This module exists only so
// the workspace compiles and carries a placeholder test today.

/** Root directory (relative to this workspace) where fixture data will live. */
export const FIXTURES_ROOT = 'toy-repo';
