{
  "//": "Conformance script for the reference STUB adapter (task P3-02). The runner (run.ts) spawns the manifest's invocation strings VERBATIM from `cwd`, so this certifies exactly what core's adapter client spawns. Paths are relative to this file's directory. The stub reads its default `./tests.json` from `cwd` (the toy repo), and `crucible-adapter-stub` resolves via the workspace bin dir on PATH. The stub passing this run is what makes the run the protocol's executable spec.",
  "name": "stub",
  "manifest": "../../adapters/stub/crucible-adapter.yaml",
  "cwd": "../toy-repo",
  "pathPrepend": "../../node_modules/.bin",
  "cases": "./cases.json",
  "timeoutMs": 15000,
  "package": {
    "files": [
      "../../adapters/stub/dist/cli.js",
      "../../adapters/stub/dist/adapter.js",
      "../../adapters/stub/crucible-adapter.yaml"
    ]
  }
}
