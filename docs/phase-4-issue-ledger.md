# Phase 4 Issue Ledger

Running record of framework and operational issues discovered while validating
Crucible with `yet-another-notes-app`. Add the resolution and verification when
an issue is fixed; do not silently remove the history.

| ID | Issue | Status | Resolution / next action |
| --- | --- | --- | --- |
| P4-001 | A consumer CI workflow could not check out the pinned private `crucible-v2` source repository. | Resolved operationally | `crucible-v2` was made public. Consumer workflows can now read the exact source pin without a project PAT. The temporary PAT and Actions secret should be removed/revoked. |
| P4-002 | The Notes project's framework pin referred to commit `e32afd9`, which existed locally but was not reachable from the public `origin/main`; CI failed with `upload-pack: not our ref`. | Resolved | Published the existing fast-forward `main` commit. Re-running PR #3 produced green `verify` and `route` checks. |
| P4-003 | A global `crucible` executable (`0.1.0`) shadowed the current built Crucible CLI and lacked `doctor`. | Resolved for validation | P4-08/P4-10 installs a project-local launcher and managed skills that invoke only `node .crucible/bin/crucible.mjs`; ambient executables are never consulted. General package distribution remains deferred. |
| P4-004 | A Java-only project’s first `propose` failed because Crucible invoked `npx openspec`, which depended on a consumer `package.json` and local OpenSpec installation. | Resolved | PR #25 packaged the exact `@fission-ai/openspec@1.6.0` runtime in `@crucible/core`; `propose`/`archive` invoke it with Node, and a regression test scaffolds from a repository with no npm project. The current safe rollout is a separately reviewed `init --framework-source owner/repository@sha` pin bump. P4-07 tracks the narrower dedicated update command. |
| P4-005 | No command auto-populates Phase 4 `docs/metrics.md`. | Deferred / non-blocking | Record rows through a post-merge agent instruction for now. Add a read-only, non-enforcement `crucible metrics` command only when manual collection becomes a real friction point. |
| P4-006 | A PAT injected into the existing `pull_request` workflow would be accessible to same-repository PR code and could be exfiltrated by a modified workflow. | Resolved by decision | Do not add PAT support to the shipped workflow. Public pinned source is the adopted bootstrap path; a private-source model requires a separate trust-boundary design. |
| P4-007 | Nested Codex propose sessions cannot use `workspace-write` on the Notes validation host: bubblewrap fails to create its loopback interface (`bwrap: loopback: Failed RTM_NEWADDR: Operation not permitted`), so the role cannot read or write and produces an empty bundle. | Resolved by P4-09 | `workspace-write` remains the default; an explicit gitignored `.crucible/local.yaml` `agent.codex_sandbox: danger-full-access` opt-in is available for the affected host. There is no automatic fallback, CI/enforcement remains unchanged, and focused config/substrate tests plus strict typechecking verify the contract. P4-08 skills are not a standalone fix because a thin wrapper would still spawn the same nested session. |
| P4-008 | On the Notes validation host, even a direct Codex `workspace-write` session cannot start filesystem commands because Bubblewrap cannot create its loopback interface. The P4-09 full-access opt-in is intentionally unacceptable to an operator who does not want to remove that boundary. | Resolved | PR #28 delivered P4-08/P4-10 session-native propose/revise and implement outside `AgentSubstrate`, with CLI-owned handoffs and unchanged approval/verify/CI authority. |
| P4-009 | The generated local launcher derived `<checkout>/core` as the framework root and then searched for `<checkout>/core/core/dist/cli/bin.js`. | Resolved | PR #29 corrected root derivation and added a regression assertion that the doubled `core/core/dist` path is never emitted. |
| P4-010 | Session-native Notes proposal authoring reached valid `oracles.md`, then raw OpenSpec offered `tasks.md` before approval while the CLI had no handoff capable of authorizing the missing bound Java test files required by the one rich gate. | Design resolved; implementation queued as P4-11 | P4-11 amends resolve with a strict `missing.candidateFile` authoring hint, adds derived `oracle-tests`/`ready` stages, suppresses OpenSpec tasks preapproval, and rejects preapproval `tasks.md` in both proposal modes and approve. Resume the existing Notes `create-note` checkpoint after the framework fix is merged and pinned. |
| P4-011 | After P4-11 created the Spring MockMvc oracle test, retrying the preserved Notes checkpoint failed in Java/JUnit `resolve` with `NoClassDefFoundError: org/springframework/test/web/servlet/RequestBuilder`; Maven and Gradle supplied only compiled output directories to the Launcher helper, omitting project test runtime dependencies. | Resolved by P4-12 (`76a0995`) | P4-12 retains discovery-only resolve and now uses build-tool-evaluated, order-preserving test classpaths: Maven effective output directories plus pinned-plugin test-scope dependencies; Gradle's configured root `Test.classpath`. Tool/model/linkage failures abort resolution with no partial fallback. Maven Spring MockMvc and customized-Gradle-classpath regressions, malformed/tool-failure tests, resolver canaries, package stability, root lint, full suite, conformance, and build are green. Preserve the Notes bundle, oracle test, and `oracle-tests` checkpoint; a separately reviewed pin bump must return ordinary discovery as both targets `found` before approval. |
| P4-012 | Notes PR #8 checked out and built its target-branch framework pin, but Node 20 failed before `verify` with `ERR_UNKNOWN_FILE_EXTENSION` because runtime workspace packages exported `src/index.ts` to the compiled CLI. | Resolved by P4-13 (PR #32) | Runtime exports now use `dist` JavaScript while the TypeScript-only declaration condition remains source-visible because CI typechecks before building. The P4-13 regression proves that split and the built CLI runs under plain-Node semantics (disabling Node 22 type stripping when present), so runtime has no source fallback. Notes pin PR #9 then moved the target branch to framework `3abd16f`, and PR #8 reached ordinary verification. |
| P4-013 | Notes PR #8 passed all deterministic verification after the P4-13 pin, but the mandatory adversarial reviewer failed with `SUBSTRATE_UNAVAILABLE`: the GitHub runner had no authenticated `codex` binary. Installing a secret in the ordinary `pull_request` job would expose it to same-repository PR code and violate the reviewer trust boundary. | Design resolved; implementation queued as P4-14 | Use architecture §11's detached target-branch reviewer: credential-free mechanical verify, inert base/head-bound request preparation, a pinned official Codex Action with the API key scoped only to that action, then a separate secretless strict core judge. Require `verify`, `review`, and `route`; every missing credential/tool/output/binding is red. Preserve Notes PR #8 until the framework implementation and isolated product pin/workflow rollout are complete. |
| P4-014 | Notes adopted the P4-14 detached reviewer, but the operator deliberately deferred paid API credentials while accepting local review as advisory only. | Design resolved; implementation queued as P4-15 | Add strict target-branch `review.ci_mode: advisory|required`; absence is required and secret absence never selects policy. Advisory keeps `verify` + `route` mandatory, makes no model call, and emits no verdict. Required remains P4-14 fail-closed. Notes must pin P4-15 and merge a risk-routed config PR before rerunning PR #8. |
| P4-015 | The exact-pin launcher rejected ordinary commands after candidate `init` staged the P4-15 pin, and a product proposal for the already-staged judge upgrade could not supply an honest new red product oracle. | Design resolved by P4-16 | Classify the framework pin plus deterministic managed harness bytes as the P4-14 isolated no-governed-change bootstrap lane. The old target harness and explicit human review judge that PR; product or config changes and invented oracles are forbidden. Remove the accidental empty proposal and checkpoint, merge the pin PR, then submit the separate risk-routed advisory-mode change. |
| P4-016 | The Notes `enable-advisory-ci-review` approval computed `standard` from the pre-implementation bundle/test diff even though its approved implementation would edit self-risk-globbed `crucible.yaml`; the charter-promised `--tier critical` was not exposed or durable into CI. | Resolved by P4-17 (pending merge/pin) | Strict `approve --tier` now persists the effective `minimum_tier`; verify takes its maximum with final target-branch facts and rejects incomplete, duplicate, or unknown critical acknowledgments. Focused plus full typecheck/lint/build/test validation are green. Merge and pin Notes before returning to its preserved unapproved bundle/test/checkpoint. |
| P4-017 | Notes has one maintainer. After P4-17 correctly forced the advisory-policy change to critical, its `route` check required a non-author GitHub approval that the project cannot supply; the existing proposal also explicitly retained `route`, so merging around it would contradict the sealed scope. | Design resolved; implementation queued as P4-18/P4-19 | Add independently explicit PR AI/human review modes selected at init, keep deterministic verification mandatory, and add a fresh snapshot-bound local reviewer loop with human-mediated red recovery. Ship both framework tasks before solo rollout. Do not merge Notes PR #13; remove future-disabled external checks, pin the merged cluster, then create a new governed critical-floor policy change while preserving `create-note`. |
| P4-018 | Notes' first required local review reached committed snapshot `743ec64`, but the child Codex reviewer repeatedly failed to write its caller-minted verdict under nested Bubblewrap (`bwrap: loopback: Failed RTM_NEWADDR`); core correctly returned `NO_VERDICT`, and the P4-19 session reduced the cause to a detail-free `review-red` checkpoint. | Design resolved; implementation queued as P4-20 | Replace only the session-native local-review transport with a CLI-minted work order consumed by the managed review skill in a separately opened interactive Codex conversation. Pinned core still binds and judges the verdict; headless and CI transports remain unchanged. Preserve the Notes branch at red, merge/pin P4-20 on target, then restart and critically reapprove the policy change because its existing seal includes the old framework lock. |
| P4-019 | Notes' P4-20 policy change passed fresh local review but exceeded the critical diff cap by 16 lines. Its narrow artifact amendment first failed because the shared judge rejected legitimate post-approval `tasks.md`; after the checklist was moved recoverably, the headless propose-role subprocess produced no regeneration under nested Bubblewrap. The operator rejects `danger-full-access`. | Ratified; implementation queued as P4-21 | Add session-native amend handoffs and a human-only seal step; preserve the old approval as the bound baseline until re-seal. Make tasks rejection phase-aware: pre-approval remains red, while amendment ignores downstream tasks. Keep the Notes implementation/oracle/checklist intact, merge and pin P4-21, then resume the amendment without a child process. |
| P4-020 | The P4-21-pinned Notes retry completed amendment, human reseal, reimplementation, and fresh review, but final verify remained 415/400. The committed `state.yaml` grew to 52 lines as mandatory commands appended audit events, so derived cache bytes influenced the diff cap despite invariant 1. | Implemented as P4-22; pending merge and pin | Filter only exact active/archive Crucible `state.yaml` paths from shared enforcement touched-path and line facts. Keep raw diffs/audit history visible, count every other byte, fail closed on malformed/tool output, merge and pin the framework correction, then restart/reapprove the Notes policy from the new target pin. |
| P4-021 | Notes PR #18 passed its sealed policy oracle, fresh local review, and P4-22 local verify, but CI returned `REVIEW_POSTURE_DRIFT` for both managed workflows. The target commit's workflow blobs are byte-identical to the pinned P4-22 templates; the CI `--config-from` directory contains only `crucible.yaml` and the framework lock, so the strict congruence judge sees both workflow paths as missing. | P4-23 merged; consumer rollout superseded by P4-24 | P4-23 materialized the complete snapshot layout and retained strict congruence, but its first Notes pin exposed a producer/consumer path mismatch before the pin could merge. Preserve PR #18 as evidence and follow P4-24 recovery rather than opening another narrow patch. |
| P4-022 | Notes P4-23 pin PR #19 failed in 14 seconds: extraction wrote `<snapshot>/.crucible/framework.lock.json`, while `Resolve pinned Crucible framework` read `<snapshot>/framework.lock.json`. The P4-23 task had required real-Git consumer, worked verification, and bootstrap tests, but implementation contained only structural substring assertions and an in-memory snapshot copy. Audit also confirmed that the `pull_request` trigger executes candidate workflow bytes, contradicting P4-16's claim that the old target workflow judges its own replacement. | Design resolved; implementation queued as P4-24 | Make the mechanical workflow base-owned and credential-free, atomically mint exact-SHA snapshot/pin outputs, recompute routing in a non-candidate-executing job, and run exact generated scripts in real-Git plus worked consumer tests. Preserve Notes PRs #8/#18/#19; no further consumer rollout occurs until P4-24 framework CI and a Notes-shaped local bootstrap simulation are green. |
| P4-023 | P4-24 merged with green framework CI, but the required Notes-shaped simulation had not run. The first clean consumer attempt showed `init -y` changes forbidden config and creates forbidden skills. Source audit found broader release blockers: every no-bundle diff passes, CI verify/route accept no approval, route no longer files override ratchet issues, advisory-human override rejection is absent, fetch assumes a fork head exists on the base remote, the legacy trigger migration is unmodeled, archive-only lifecycle is unclassified, and most P4-24 acceptance bullets/manifest/worked consumers were never implemented. | P4-25 ratified; Terra implementation in progress | Ratify and implement one release-qualified authority boundary, dedicated transactional upgrade command, explicit legacy trigger transition, strict typed PR classifier, CI-only approval-required commands, restored override ratchet/posture law, fork/archive coverage, and an executable acceptance manifest. Do not open another Notes pin/policy/product PR until the built-candidate Notes mirror completes the full transition and fresh flows. |

| P4-024 | The first P4-25 Notes authority-transition PR correctly built the target P4-22 framework pin, which has no `ci authority` command. A candidate-owned workflow cannot become target-owned merely by running a newer candidate pin. | Resolved by P4-25 legacy-bootstrap amendment; implementation in progress | Treat phase one as an explicitly acknowledged manual two-file root bootstrap with no authority claim or required verify check. After that bridge merges, an exact same-pin workflow-only finalization is classified by target-owned authority. Reject bridge drift and repins; preserve Notes PRs #20 and #21 as failure evidence. |

| P4-025 | Notes PR #23 proved P4-25 target-owned authority, verification, and route are healthy after the bridge, but its detached reviewer correctly failed because the preserved target has no review block and therefore defaults CI review to required. No API key is authorized. Candidate config cannot select advisory for itself, and a red or empty judge cannot be merged or treated as advisory. | Implemented as P4-26; pending merge | Add one explicit P4-26 manual solo-posture root bootstrap from the exact post-#22 bridge only. It stages an allowlisted pin/workflow/config/settings transaction, requires acknowledgement plus manual comparison and local review, and ends at target-owned advisory/advisory posture with required local review. It never weakens required mode or creates a generic config bypass. Close #23 as superseded; preserve #8/#18. |

## Resolution protocol

For a framework issue, fix it in `crucible-v2` under the normal test-first
workflow, update the relevant design/runbook documentation if a contract changes,
and record the fix commit/PR plus verification result in this ledger. Consumer
projects receive the fix through a separately reviewed framework-pin bump.
