{
  "name": "@crucible/ci-templates",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Reusable CI templates (e.g. crucible.yml) that enforce verify on the target branch. Shipped as data, validated by tests.",
  "//": "The built core CLI loads this package under plain Node in consumer CI, so its runtime export is dist/index.js. `crucible init` copies the crucible.yml data into a target repo's .github/workflows/.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc"
  }
}
